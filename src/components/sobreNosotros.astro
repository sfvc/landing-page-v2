---
import { catamarcaApi } from "../api/catamarcaApi";
import {
  getDatos,
  getDatosImagenes,
  getDatosRedesSociales,
} from "../util/obtenerDatos.astro";

const datosCompletos = await getDatos();
const redesSociales = await getDatosRedesSociales();
const imagenes = await getDatosImagenes();
---

<section id="sobre-nosotros" class="py-16 bg-white">
  <div class="container mx-auto px-4">
    <h2
      class="text-3xl font-bold text-center mb-12 colorPrincipalTexto reveal-title"
    >
      Sobre Nosotros
    </h2>
    <div class="flex flex-col md:flex-row items-center">
      <div class="md:w-1/2 mb-8 md:mb-0 reveal-left">
        <img
          src={`${catamarcaApi.defaults.baseURL}/assets/${imagenes[0].sobre_nosotros}`}
          alt={`Sobre ${datosCompletos.nombre}`}
          class="rounded-lg shadow-md w-full transform transition-transform duration-500 hover:scale-105"
        />
      </div>
      <div class="md:w-1/2 md:pl-12 reveal-right">
        <p
          class="text-lg mb-6 text-justify leading-relaxed tracking-normal hyphens-auto max-w-prose"
        >
          El {datosCompletos.nombre} es un espacio municipal de la Dirección de Juventud
          de la Municipalidad de San Fernando del Valle de Catamarca, destinado a
          jóvenes de 15 a 35 años.
        </p>
        <p
          class="text-lg mb-6 text-justify leading-relaxed tracking-normal hyphens-auto max-w-prose"
        >
          Ofrece talleres gratuitos de formación (robótica, programación,
          oratoria, muralismo, etc.), espacios de estudio con internet y
          equipamiento multimedia, acompañamiento psicológico y orientación
          vocacional, así como actividades recreativas, culturales y deportivas.
        </p>
        <p
          class="text-lg text-justify leading-relaxed tracking-normal hyphens-auto max-w-prose"
        >
          Funciona en Av. Máximo Victoria 143, de lunes a viernes de 8:00 a
          13:00 y de 15:00 a 20:00, según la programación de cada taller.
          Seguinos en
          <a
            href={redesSociales[0].url}
            target="_blank"
            class="colorPrincipalTexto colorPrincipalTextoHover underline"
            >Facebook</a
          >
          e
          <a
            href={redesSociales[1].url}
            target="_blank"
            class="colorPrincipalTexto colorPrincipalTextoHover underline"
            >Instagram</a
          >.
        </p>
      </div>
    </div>
  </div>
</section>
